apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: service6-ingress
  annotations:
    kubernetes.io/ingress.class: "nginx"
spec:
  rules:
    - http:
        paths:
          - path: /api/service6
            pathType: "Prefix"
            backend:
              service:
                name: service6-service
                port:
                  number: 80